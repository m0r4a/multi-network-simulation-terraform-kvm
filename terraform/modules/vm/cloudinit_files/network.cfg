version: 2
ethernets:
%{ for idx, iface in interfaces_v2 ~}
  enp${idx + 1}s0:
    dhcp4: false
    addresses:
      - "${iface.address}/${iface.cidr}"
%{ if iface.gateway != "" }
    gateway4: ${iface.gateway}
%{ endif }
%{ endfor ~}
